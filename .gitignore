# dependencies
node_modules/
.pnpm-store/

# build outputs
.next/
out/
dist/
build/
.astro/

# next.js
*.tsbuildinfo
next-env.d.ts

# env — never commit real credentials; keep *.example templates tracked
.env
.env.*
!.env.example
!.env.prod.example
!**/.env.example
!**/.env.demo.example
.env.local
.env.*.local
*.tmp_review

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors / OS
.DS_Store
.idea/
.vscode/

# git worktrees
.worktrees/
.claude/worktrees/
.claire/

# superpowers runtime state
.superpowers/
.claude/skills/integration-nextjs-app-router/references
.claude/scheduled_tasks.lock

# repomix
repomix-output.xml

# playwright-cli artifacts
.playwright-cli/

# §3.7 print QA — local storage state + generated PDFs/screenshots
apps/web/tests/print/.storage.json
apps/web/tests/print/output/

# M03 preloved Playwright — reports / local storage state
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/
apps/web/tests/preloved/.storage.json

# Demo assets — generated artifacts and secrets
demo/launch_video/
demo/product_demo/recordings/output/
demo/demo_data/.env.demo
*.webm
*.mp4
# Launch-video generated and captured assets; keep the editable source files


# Local Hostinger deploy archives — do not commit
.deploy/

# mcp
.mcp.json

# Machine-local GQ pins (one absolute path line each)
.gq-spec/capability-docs-path
.gq-spec/master-app-dev-process-path

# HeroUI credentials — keep local only
HEROUI_PERSONAL_TOKEN.txt
**/HEROUI_PERSONAL_TOKEN.txt

# Self-ver live captures (host via process 1300, then wipe — not a repo archive)
.self-ver/

# Per-worktree recorded origin (process 0100). Do not commit the live URL.
.dev-local/web.url

# Skill junk
**/__pycache__/
**/*.pyc
