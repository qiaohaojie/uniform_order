"use client";
import type { TenantRow } from "@/db/schema";

export function Step3Stripe({ tenant, onContinue }: { tenant: TenantRow; onContinue: () => void }) {
  return <div>Step 3 stub — see Task 4.7</div>;
}
