"use client";
import type { TenantRow } from "@/db/schema";

export function Step2Branding({ tenant, onContinue }: { tenant: TenantRow; onContinue: () => void }) {
  return <div>Step 2 stub — see Task 4.6</div>;
}
