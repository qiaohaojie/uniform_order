"use client";
import type { TenantRow } from "@/db/schema";

export function Step4Operator({ tenant, onContinue }: { tenant: TenantRow; onContinue: () => void }) {
  return <div>Step 4 stub — see Task 4.8</div>;
}
