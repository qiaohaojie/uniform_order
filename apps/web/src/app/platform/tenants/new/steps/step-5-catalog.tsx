"use client";
import type { TenantRow } from "@/db/schema";

export function Step5Catalog({ tenant, onContinue }: { tenant: TenantRow; onContinue: () => void }) {
  return <div>Step 5 stub — see Task 4.9</div>;
}
