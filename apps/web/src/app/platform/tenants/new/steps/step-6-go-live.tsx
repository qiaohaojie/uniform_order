"use client";
import type { TenantRow } from "@/db/schema";

export function Step6GoLive({ tenant, catalogCount }: { tenant: TenantRow; catalogCount: number }) {
  return <div>Step 6 stub — see Task 4.10</div>;
}
